[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cc-usr-care-consent"
version = "0.1.0"
description = "陪护机器人记忆同意领域契约"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]